namespace LiberationFleet.Server.Domain.Enums;

/// <summary>
/// Category selected when recording a custom gift (outside reception order).
/// </summary>
public enum CustomGiftCategory
{
    Cycle = 1,
    SurvivalThreshold = 2,
    Emergency = 3,
    Other = 4
}
